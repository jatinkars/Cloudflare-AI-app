{
  "name": "cf_ai_edge_chat_memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev --persist"
  },
  "devDependencies": {
    "wrangler": "^3.80.0",
    "typescript": "^5.6.3"
  }
}